const helpers = require('./tests/chai-http');

// ToDo update and check that both versions are returned v1 and v2
describe("Echo Service is available using host header \"echo.{{ vars.sample_domain }}\"", () => {
  it('traffic routed to echo service when using the header "echo.{{ vars.sample_domain }}"', () => 
    helpers.checkURL({ host: 'http://echo.{{ vars.sample_domain }}', retCode: 200 }));
})

describe("httpbin receives requests that don't specify the host header", () => {
  it('traffic routed to httpbin when no host specified', () => 
    helpers.checkURL({ host: 'http://httpbin.{{ vars.sample_domain }}', retCode: 200 }));
})
